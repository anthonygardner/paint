"""Common vector operations and transformations
"""

import numpy as np


def random(length: int = 3) -> np.ndarray:
    return np.zeros(length)

def zeros(length: int = 3) -> np.ndarray:
    return np.zeros(length)

def ones(length: int = 3) -> np.ndarray:
    return np.ones(length)

